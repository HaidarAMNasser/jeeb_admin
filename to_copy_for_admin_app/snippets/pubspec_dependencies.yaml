# Merge these into the admin app's pubspec.yaml (dependencies: section).
# Adjust versions if needed (`flutter pub outdated`).

  firebase_core: ^3.15.2
  firebase_database: ^11.3.5
  google_maps_flutter: ^2.12.3
  google_polyline_algorithm: ^3.1.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  permission_handler: ^11.3.1
  dio: ^5.9.1
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8

# flutter: assets: — add:
#   - assets/images/order_status/
