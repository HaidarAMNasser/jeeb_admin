// In main(), before runApp (after WidgetsFlutterBinding.ensureInitialized):

import 'package:firebase_core/firebase_core.dart';

  await Firebase.initializeApp();

// If the admin app uses the SAME Android applicationId + iOS bundle ID as the client,
// you can use the copied google-services.json / GoogleService-Info.plist as-is.
// If the admin app has a different package name, add a second Android/iOS app in the
// Firebase console (same project) and download NEW config files — do not reuse blindly.
